# 最终风险检查清单

## 1. Token / .env 泄漏

- **风险**：API Key 被提交到仓库
- **状态**：✅ 安全。DeepSeekProvider 只读环境变量，`.gitignore` 已忽略 `.env`。代码中无硬编码 token
- **是否必须修复**：否
- **建议**：展示前确认 `git status` 无 `.env`

## 2. outputs/demo 运行产物提交

- **风险**：demo 输出文件被意外提交
- **状态**：✅ 已配置。`.gitignore` 忽略 `outputs/demo/*.md`
- **是否必须修复**：否
- **建议**：展示前确认 `outputs/demo/` 下只有运行产物

## 3. README 准确性

- **风险**：README 描述的已完成功能与代码实际不符
- **状态**：✅ 准确。测试数 216、Streamlit/Evaluation/ModelProvider 均已标注完成
- **是否必须修复**：否
- **建议**：每次新增功能后同步更新 README

## 4. Demo 可复现性

- **风险**：CLI demo 在目标环境无法运行
- **状态**：✅ 可复现。零外部依赖，CLI 仅需 Python 3.10+ 和 pytest
- **是否必须修复**：否
- **建议**：展示前在目标环境运行一次确认

## 5. 测试通过

- **风险**：展示时测试失败
- **状态**：✅ 216 passed
- **是否必须修复**：否
- **建议**：展示前运行一次全量测试

## 6. 过度包装

- **风险**：把项目描述为完整产品
- **状态**：✅ 已检查。所有文档均标注"第一阶段 MVP""原型""规则型"
- **是否必须修复**：否
- **建议**：展示时强调"这是一阶段原型，后续可升级"

## 7. 错误声称已接入真实 LLM

- **风险**：说 BuildAgent 已用 LLM 但其实默认规则型
- **状态**：✅ 已检查。文档明确写"可选 LLM，默认规则型"
- **是否必须修复**：否
- **建议**：如果说"支持 LLM"，必须补充"需配置 API Key，默认规则型"

## 8. 错误声称已实现 Embedding

- **风险**：说已用语义检索但其实关键词匹配
- **状态**：✅ 已检查。文档明确写"内存关键词检索"
- **是否必须修复**：否
- **建议**：说"检索"时补充"当前为关键词匹配"

## 9. 错误声称已实现 LangGraph

- **风险**：说 workflow 基于 LangGraph
- **状态**：✅ 已检查。文档明确写"普通 Python workflow，后续可迁移 LangGraph"
- **是否必须修复**：否
- **建议**：AgentTaskState 设计对齐 LangGraph，但不声称已使用

## 10. 学校服务器展示

- **风险**：服务器环境不满足运行条件
- **状态**：⚠️ 需现场验证。Python 版本、端口、网络需提前检查
- **是否必须修复**：否，但建议提前验证
- **建议**：
  - 提前在服务器上 clone 并运行测试
  - 准备 CLI 备用方案（无需端口）
  - 准备截图备用（极端情况下用截图讲解）

## 总结

**10 项检查全部通过或已有应对。无必须修复的阻塞问题。**
